{
	"$schema": "http://json-schema.org/draft-07/schema",
	"definitions": {
		"refresh": {
			"$id": "#refresh",
			"title": "Refresh token",
			"description": "JWT refresh token to generate new JWTs",
			"type": "object",
			"properties": {
				"refresh": {
					"description": "Refresh token",
					"type": "string"
				}
			},
			"required": ["refresh"],
			"additionalProperties": false
		}
	}
}
